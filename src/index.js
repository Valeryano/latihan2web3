import React from 'react';
import ReactDOM from 'react-dom';
import App from './App'; // Impor komponen App

ReactDOM.render(
  <React.StrictMode>
    <App /> {/* Gunakan komponen App di sini */}
  </React.StrictMode>,
  document.getElementById('root')
);
